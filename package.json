{
  "name": "keeperlive",
  "version": "1.0.0",
  "description": "Uptime keeper: periodically checks registered URLs and emails recipients when a site is unreachable. URLs and recipients are managed via a backoffice without restarting the server.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-cron": "^3.0.3",
    "nodemailer": "^6.9.14"
  }
}
